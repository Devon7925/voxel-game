Abiliy Modifiers
    charges
    add cast time
    shared cooldowns
    keybind
        Pressed (button)
        On Pressed (button)
        Released (button)
        On Released (button)
        And (keybind, keybind)
On Trigger
    On Trigger (id)
    Trigger (id)
Add effect ui
Finish card editor
    allow dragging
        multi modifiers
        base cards
    select palette type
    scrolling
    clip out of area
